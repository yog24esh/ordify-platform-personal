package com.ordify.admin.common.session;
//
////import com.ordify.user.entity.User;
//
//import java.util.Optional;
//
///**
// * SessionService
// *
// * Provides operations related to user session management.
// * Used by interceptors, validators, and services to resolve logged-in users.
// */
public interface SessionService {
//
//    // Returns the user associated with a given session ID
////    Optional<User> getUserBySessionId(String sessionId);
//
//    // Creates a new session for a user and returns the session ID
//    String createSession(Long userId);
//
//    // Invalidates an existing session
//    void invalidateSession(String sessionId);
//
//    // Checks whether a session is still valid
//    boolean isSessionValid(String sessionId);
}
